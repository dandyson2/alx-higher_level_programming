// Wait for the DOM content to be fully loaded and ready
document.addEventListener('DOMContentLoaded', function () {
  // Select the first <header> element and change its text color to red (#FF0000)
  document.querySelector('HEADER').style.color = '#FF0000';
});
